# Документация: Clearing database via a temporary node

Набор скриптов для очистки баз данных ноды с минимальным временем простоя путем использования временной ноды.

## Требования

**Рабочая директория:** Все команды должны запускаться из директории `gonka/deploy/join`

```bash
cd gonka/deploy/join
```

**Необходимые инструменты:**
- Docker и Docker Compose
- `jq` для парсинга JSON
- `yq` для парсинга YAML (установка: `sudo wget https://github.com/mikefarah/yq/releases/latest/download/yq_linux_amd64 -O /usr/bin/yq && sudo chmod +x /usr/bin/yq`)

## Обзор процесса

1. **start-temp.sh** - запуск временной ноды
2. **wait-temp-sync.sh** - мониторинг синхронизации временной ноды
3. **swap-from-temp.sh** - замена состояния основной ноды на синхронизированное

## Скрипты

### 1. start-temp.sh

Создает и запускает временную ноду для синхронизации.

**Использование:**
```bash
sudo ./temp-node/start-temp.sh [--from-scratch]
```

**Опции:**
- `--from-scratch` - синхронизация с нуля из сети (без локальных снэпшотов)

**Два режима работы:**

#### Режим с локальными снэпшотами (по умолчанию)
```bash
sudo ./temp-node/start-temp.sh
```
- ✅ Быстрая синхронизация (от последнего снэпшота)
- ⚠️ Останавливает основную ноду на время копирования БД (обычно 2-10 минут)
- 📋 Копирует: `blockstore.db`, `state.db`, снэпшоты
- 🎯 Рекомендуется для быстрого восстановления

#### Режим с синхронизацией из сети
```bash
sudo ./temp-node/start-temp.sh --from-scratch
```
- ✅ Почти нулевой downtime основной ноды (несколько секунд)
- ⏱️ Долгая синхронизация (часы/дни, загружает снэпшоты из сети)
- 📋 Копирует только конфиг (без БД)
- 🎯 Рекомендуется когда downtime критичен

**Что делает:**
- Создает `.inference-temp/` и `.tmkms-temp/`
- Генерирует `docker-compose.temp.yml`
- Запускает временную ноду на портах 15001 (P2P), 26667 (RPC)

### 2. wait-temp-sync.sh

Мониторит синхронизацию временной ноды с основной.

**Использование:**
```bash
./temp-node/wait-temp-sync.sh
```

**Параметры (через переменные окружения):**
- `HEIGHT_DIFF_THRESHOLD=5` - допустимая разница в блоках (по умолчанию 5)
- `POLL_INTERVAL=5` - интервал проверки в секундах (по умолчанию 5)

**Пример:**
```bash
# Ждать пока разница не станет < 3 блоков
HEIGHT_DIFF_THRESHOLD=3 ./temp-node/wait-temp-sync.sh
```

**Вывод:**
```
[INFO] Temp height=12345 catching_up=true | Main height=12350 catching_up=false | diff=5
[INFO] Temp height=12348 catching_up=false | Main height=12350 catching_up=false | diff=2
[INFO] Height diff < 5. Done.
```

### 3. swap-from-temp.sh

Заменяет состояние основной ноды на синхронизированное состояние временной.

**Использование:**
```bash
sudo ./temp-node/swap-from-temp.sh [--copy-temp]
```

**Опции:**
- `--copy-temp` - копировать состояние временной ноды вместо перемещения (Не рекомендуется, долго)

**Безопасность:**
- ⚠️ Проверяет, что PoC-фаза неактивна (иначе отменяет операцию)
- ⚠️ Отменяет операцию, если бэкап уже существует
- 💾 Создает бэкап: `.inference/data.bak`, `.inference/wasm.bak`

**Что делает:**
1. Проверяет статус PoC через API: `http://127.0.0.1:8000/v1/epochs/latest`
2. Останавливает основную и временную ноды
3. Создает бэкап текущего состояния
4. Переносит/копирует `data/` и `wasm/` из временной ноды
5. Запускает основную ноду с новым состоянием
6. Оставляет временную ноду остановленной

## Полный workflow

### Вариант 1: Быстрая синхронизация (с остановкой основной ноды)

```bash
# 1. Запустить временную ноду (остановит основную на 2-10 мин)
sudo ./temp-node/start-temp.sh

# 2. Дождаться синхронизации
./temp-node/wait-temp-sync.sh

# 3. Переключиться на синхронизированное состояние
sudo ./temp-node/swap-from-temp.sh
```

**Время:**
- Downtime основной ноды: 2-10 минут (копирование БД)
- Синхронизация временной: 10-30 минут (от последнего снэпшота)
- Общее время: ~15-40 минут

### Вариант 2: Zero-downtime синхронизация (из сети)

```bash
# 1. Запустить временную ноду (основная продолжает работать)
sudo ./temp-node/start-temp.sh --from-scratch

# 2. Дождаться синхронизации (может занять часы)
./temp-node/wait-temp-sync.sh

# 3. Переключиться на синхронизированное состояние
sudo ./temp-node/swap-from-temp.sh
```

**Время:**
- Downtime основной ноды: несколько секунд (только swap)
- Синхронизация временной: несколько часов/дней (из сети)
- Общее время: зависит от размера блокчейна

## Переменные окружения

Настройка через `config.env` или прямой export:

```bash
# Директории
MAIN_HOME=".inference"
TEMP_HOME=".inference-temp"
TEMP_TMKMS_HOME=".tmkms-temp"

# Порты временной ноды
TEMP_P2P_PORT=15001
TEMP_RPC_PORT=26667

# Имена контейнеров
TEMP_CONTAINER="node-temp"
TEMP_PROJECT_NAME="gonka-temp"

# Проверка PoC
POC_CHECK_URL="http://127.0.0.1:8000/v1/epochs/latest"

# Мониторинг синхронизации
HEIGHT_DIFF_THRESHOLD=5
POLL_INTERVAL=5
```

## Откат изменений

Если что-то пошло не так после swap:

```bash
# Остановить основную ноду
docker stop node

# Восстановить бэкап
cd .inference
rm -rf data wasm
mv data.bak data
mv wasm.bak wasm

# Запустить основную ноду
docker start node
```

Или использовать `./temp-node/rollback-swap.sh` (если доступен).

## Очистка

После успешного swap и проверки работы:

```bash
# Удалить временные файлы
rm -rf .inference-temp
rm -rf .tmkms-temp
rm -f docker-compose.temp.yml

# Удалить временные контейнеры
docker compose -p gonka-temp down -v

# Удалить бэкапы (когда уверены, что всё работает)
rm -rf .inference/data.bak
rm -rf .inference/wasm.bak
```

## Troubleshooting

**Временная нода не запускается:**
```bash
# Проверить логи
docker logs node-temp

# Проверить порты
netstat -tulpn | grep -E "(15001|26667)"
```

**Синхронизация застряла:**
```bash
# Проверить статус
curl http://127.0.0.1:26667/status | jq .result.sync_info
curl http://127.0.0.1:26657/status | jq .result.sync_info
```

**Swap отменяется из-за PoC:**
```bash
# Проверить статус PoC
curl http://127.0.0.1:8000/v1/epochs/latest | jq .is_confirmation_poc_active

# Дождаться окончания PoC-фазы и повторить swap
```

## Рекомендации

1. **Используйте локальные снэпшоты** если downtime 2-10 минут допустим
2. **Используйте --from-scratch** если требуется максимальная доступность
3. **Мониторьте PoC-фазы** - swap запрещен во время PoC
4. **Сохраняйте бэкапы** до полной проверки работы ноды
5. **Тестируйте процесс** на тестовой ноде перед продакшеном
